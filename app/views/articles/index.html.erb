<h1>Listing articles</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Body</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @articles.each do |article| %>
  <tr>
    <td><%= article.name %></td>
    <td><%= article.body %></td>
    <td><%= link_to 'Show', article %></td>
    <td><%= link_to 'Edit', edit_article_path(article) %></td>
    <td><%= link_to 'Destroy', article, confirm: 'Are you sure?', method: :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Article', new_article_path %><br /><br />
<%= link_to 'Create A New Article called Edward', articles_path(:article => {:name => "Edward"}), :method => :post %>
<%= link_to 'Change name of article id 1', article_path(:id => 1, :article => {:name => "new name"}), :method => :put %>


