      <%#= image_tag "delete.png", class: "delete-child_link", id: link.id %>
      <%= link_to (image_tag "delete.png"), link, method: :delete, remote: :true, class: 'delete-child-link' %>
      <%= link_to (image_tag Article.find(link.child_id).image, size: "80x80"), Article.find(link.child_id) %>